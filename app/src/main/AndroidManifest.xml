<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a4.cis350.when2meetmobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeScreenActivity" />
        <activity android:name=".AddEventActivity" />
        <activity android:name=".EventSettingsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".WeeklyTimePickerActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".AvailabilitiesActivity" />
        <activity
            android:name=".PopupCalActivity"
            android:label="@string/title_activity_popup_cal"
            android:theme="@style/MyMaterialTheme" />
        <activity android:name=".CalendarTimePickerActivity" />
        <activity android:name=".InviteActivity" />
        <activity android:name=".InviteUsersActivity" />
        <activity android:name=".InvitesScreenActivity" />
        <activity android:name=".ProfileSetup"></activity>
    </application>

</manifest>